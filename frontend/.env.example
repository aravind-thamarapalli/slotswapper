# Frontend environment variables example
# Copy this to .env (do NOT commit real secrets) and edit as needed.

# Backend API base URL (no trailing /api required). In production this should point to the deployed backend:
VITE_API_URL=https://slotswapper-bknd.vercel.app

# WebSocket URL (optional). If you want explicit wss/ws use wss://...
VITE_WS_URL=wss://slotswapper-bknd.vercel.app

# Frontend public URL (deployed site)
VITE_CLIENT_URL=https://slotswapper-pink.vercel.app

# Notes:
# - The app prefers VITE_API_URL / VITE_WS_URL when present. If not provided it falls back
#   to the production URL above. For local development, set VITE_API_URL=http://localhost:5000
# - Never commit credentials. Use environment variables in your hosting platform (Vercel, Netlify, etc.)
